import Layout from "Layouts/Layout";
import { NextPage } from "next";

const Home: NextPage = () => {
    return (
        <Layout>
            <div className="w-full text-yellow-500 font-semibold">HOME</div>
        </Layout>
    );
};

export default Home;
